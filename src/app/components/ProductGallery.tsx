"use client";
import { useState } from "react";
export function ProductGallery({ images, productName }: { images: string[]; productName: string }) { const [active,setActive]=useState(images[0]??""); return <div>{active&&<div className="overflow-hidden border bg-white"><img src={active} alt={`Imagen de ${productName}`} className="h-[32rem] w-full object-contain transition-transform duration-300 hover:scale-150"/></div>}<div className="mt-3 flex gap-3">{images.map((url,index)=><button key={`${url}-${index}`} type="button" aria-label={`Ver imagen ${index + 1} de ${productName}`} onClick={()=>setActive(url)} className={url===active?"border-2 border-[#FF46A2]":"border"}><img src={url} alt={`${productName}, imagen ${index + 1}`} className="h-20 w-20 object-cover"/></button>)}</div></div> }
