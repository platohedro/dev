"use client";
import { useTranslation } from "react-i18next";
export function ProductBuyButton({ productId, disabled }: { productId: string; disabled: boolean }) { const { t } = useTranslation(); return <button disabled={disabled} onClick={async()=>{const r=await fetch("/api/wompi/checkout",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({productId})});const d=await r.json();if(!r.ok)return alert(d.error);const f=document.createElement("form");f.method="GET";f.action=d.checkoutUrl;Object.entries(d.fields).forEach(([n,v])=>{const i=document.createElement("input");i.type="hidden";i.name=n;i.value=String(v);f.append(i)});document.body.append(f);f.submit()}} className="mt-5 bg-[#99CC33] px-4 py-3 font-bold text-[#003d7a] disabled:opacity-50">{disabled?t("productBuyButton.soldOut"):t("productBuyButton.buy")}</button> }
