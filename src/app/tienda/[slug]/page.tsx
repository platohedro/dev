import { notFound } from "next/navigation"; import { SiteHeader } from "@/app/components/SiteHeader"; import { ProductGallery } from "@/app/components/ProductGallery"; import { createSupabaseServerClient } from "@/lib/supabase/server";
export const dynamic="force-dynamic";
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=await createSupabaseServerClient();const {data:p}=await s.from("products").select("*").eq("slug",slug).eq("is_published",true).maybeSingle();if(!p)notFound();const {data:gallery}=await s.from("product_images").select("image_url,position").eq("product_id",p.id).order("position");const images=[p.image_url,...(gallery??[]).map(i=>i.image_url)].filter(Boolean) as string[];return <main className="min-h-screen bg-background"><SiteHeader/><div className="mx-auto grid max-w-6xl gap-10 p-8 md:grid-cols-2"><ProductGallery images={images}/><section><h1 className="text-4xl font-bold">{p.name}</h1><p className="mt-5 text-muted-foreground">{p.description}</p><p className="mt-8 text-2xl font-bold">${Number(p.price_cop).toLocaleString("es-CO")} COP</p><p className="text-muted-foreground">US$ {p.price_usd} · Tasa: ${p.exchange_rate} COP/USD</p><p className="mt-8 border border-[#99CC33] bg-[#99CC33]/10 p-4 text-sm">Compra en línea próximamente. Por ahora puedes explorar la galería del producto.</p></section></div></main>}
